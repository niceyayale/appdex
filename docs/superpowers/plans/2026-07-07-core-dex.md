# appdex 子项目 #4:`:core:dex` DEX 只读解析 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现基于 dexlib2 的 DEX 只读解析模块,能列出 DEX 内所有类、类的字段与方法、将单个类反汇编为 smali 文本。JVM 单测可验证。

**Architecture:** `:core:dex` KMP 模块,commonMain 定义 `DexReader` 接口与数据类,jvmMain 用 dexlib2(`org.smali:dexlib2:3.0.5`)+ smali(`org.smali:smali:3.0.5`)实现。测试用 dexlib2 的 Immutable API 动态构造最小 DEX 字节,自包含无需外部样本。

**Tech Stack:** Kotlin 2.0.21 KMP,dexlib2 3.0.5,smali 3.0.5,JUnit 5。

**环境前置:**
- JDK 17 路径 `/root/.local/share/mise/installs/java/17.0.2`
- 所有 gradle 命令前设 `JAVA_HOME=/root/.local/share/mise/installs/java/17.0.2`
- 代理已配,git 提交后必须 push
- 工作目录 `/workspace`

**范围(只读 MVP):**
- 打开 DEX、列出类、字段、方法
- 单类反汇编为 smali 文本
- **不包含**:DEX 编辑、smali 汇编、指令级修改

---

## 关键 API 参考(dexlib2 3.0.5)

执行时如 API 与下列不符,以实际为准(查 dexlib2 源码或 javadoc):

```kotlin
// 加载 DEX
import org.jf.dexlib2.Opcodes
import org.jf.dexlib2.dexbacked.DexBackedDexFile
val dexFile = DexBackedDexFile(Opcodes.getDefault(), dexBytes)

// 遍历类
for (cls in dexFile.classes) {
    val type = cls.type              // "Lcom/example/Test;"
    val accessFlags = cls.accessFlags
    val superclass = cls.superclass  // "Ljava/lang/Object;"
    val sourceFile = cls.sourceFile
    // 接口
    cls.interfaces                   // List<String>
    // 字段
    for (field in cls.fields) {
        field.name
        field.type
        field.accessFlags
    }
    // 方法
    for (method in cls.methods) {
        method.name
        method.parameterTypes
        method.returnType
        method.accessFlags
    }
}

// smali 反汇编
import org.jf.smali.SmaliWriter
import org.jf.dexlib2.writer.io.StringDataStore  // 或 MemoryDataStore
// 用 SmaliWriter 写出 smali 文本
```

构造 DEX 字节(测试用):
```kotlin
import org.jf.dexlib2.immutable.ImmutableDexFile
import org.jf.dexlib2.immutable.ImmutableClassDef
import org.jf.dexlib2.iface.reference.StringReference
// ImmutableClassDef 构造后用 DexPool 或 DexWriter 序列化
// Task 1 spike 会验证确切 API
```

---

## 文件结构

```
/workspace/core/dex/
├── build.gradle.kts
└── src/
    ├── commonMain/kotlin/io/appdex/core/dex/
    │   └── DexReader.kt              (接口 + 数据类)
    ├── jvmMain/kotlin/io/appdex/core/dex/
    │   └── BinaryDexReader.kt         (dexlib2 实现)
    ├── jvmTest/kotlin/io/appdex/core/dex/
    │   ├── DexSample.kt              (构造最小 DEX 字节)
    │   ├── BinaryDexReaderTest.kt
    │   └── SmaliTest.kt
```

---

## Task 1:模块骨架 + dexlib2 依赖 + API spike

**Files:**
- Modify: `/workspace/settings.gradle.kts`
- Create: `/workspace/core/dex/build.gradle.kts`
- Create: `/workspace/gradle/libs.versions.toml`(加 dexlib2)
- Create: `/workspace/core/dex/src/jvmTest/kotlin/io/appdex/core/dex/DexlibSpikeTest.kt`

- [ ] **Step 1: 在 settings.gradle.kts 加模块**

```kotlin
include(":core:dex")
```

- [ ] **Step 2: 在 libs.versions.toml 加依赖**

`[versions]` 加:
```toml
dexlib2 = "3.0.5"
```

`[libraries]` 加:
```toml
dexlib2 = { group = "org.smali", name = "dexlib2", version.ref = "dexlib2" }
smali = { group = "org.smali", name = "smali", version.ref = "dexlib2" }
```

- [ ] **Step 3: 写 core/dex/build.gradle.kts**

```kotlin
plugins {
    alias(libs.plugins.kotlin.multiplatform)
}

kotlin {
    jvm {
        compilations.all {
            kotlinOptions.jvmTarget = "17"
        }
        testRuns["test"].executionTask.configure {
            useJUnitPlatform()
        }
    }

    sourceSets {
        commonMain {
            dependencies {
                implementation(project(":core:io"))
            }
        }
        commonTest {
            dependencies {
                implementation(libs.junit.jupiter.api)
                implementation(libs.junit.jupiter.params)
                runtimeOnly(libs.junit.jupiter.engine)
            }
        }
        jvmMain {
            dependencies {
                implementation(libs.dexlib2)
                implementation(libs.smali)
            }
        }
        jvmTest {
            dependencies {
                implementation(libs.dexlib2)
                implementation(libs.smali)
            }
        }
    }
}
```

- [ ] **Step 4: 写 spike 测试(验证 dexlib2 API 可用)**

```kotlin
package io.appdex.core.dex

import org.jf.dexlib2.Opcodes
import org.jf.dexlib2.dexbacked.DexBackedDexFile
import org.jf.dexlib2.iface.ClassDef
import org.jf.dexlib2.immutable.ImmutableClassDef
import org.jf.dexlib2.immutable.ImmutableDexFile
import org.jf.dexlib2.writer.io.MemoryDataStore
import org.jf.dexlib2.writer.pool.DexPool
import org.junit.jupiter.api.Assertions.*
import org.junit.jupiter.api.Test

/**
 * spike:验证 dexlib2 关键 API 可用,并找到"构造 DEX 字节"的可行方式。
 * 后续测试用同样的方式构造样本。
 */
class DexlibSpikeTest {

    @Test
    fun `dexlib2 loads a dex byte array`() {
        val dexBytes = buildMinimalDex()
        val dexFile = DexBackedDexFile(Opcodes.getDefault(), dexBytes)
        // 能遍历类即通过
        val classes = dexFile.classes.toList()
        assertTrue(classes.isNotEmpty())
        assertEquals("Lcom/example/Test;", classes[0].type)
    }

    /**
     * 构造最小 DEX:单个空类 com.example.Test extends Object。
     * 用 ImmutableClassDef + DexPool 序列化。
     */
    fun buildMinimalDex(): ByteArray {
        val classDef = ImmutableClassDef(
            "Lcom/example/Test;",                       // type
            1,                                           // accessFlags = PUBLIC
            "Ljava/lang/Object;",                        // superclass
            null,                                        // interfaces
            "Test.java",                                 // sourceFile
            null,                                        // annotations
            null,                                        // fields
            null,                                        // methods
            null,                                        // static initializer
        )
        val dexFile = ImmutableDexFile(Opcodes.getDefault(), listOf(classDef))
        val store = MemoryDataStore()
        DexPool(Opcodes.getDefault()).apply {
            internClass(classDef)
            writeTo(store)
        }
        return store.data
    }
}
```

注:`ImmutableClassDef` 构造函数参数和 `DexPool.writeTo` 的确切签名需要验证。如果 API 不符:
- `ImmutableClassDef` 可能用 builder 模式或不同参数顺序
- `DexPool.writeTo` 接受 `DataStore` 接口
- `MemoryDataStore.data` 可能叫 `getData()` 或字段 `data`

**spike 失败时**:查 dexlib2 3.0.5 源码(在 `~/.gradle/caches/modules-2/files-2.1/org.smali/dexlib2/` 下的 jar,用 `jar tf` + `javap` 看签名),调整 `buildMinimalDex` 直到测试通过。

- [ ] **Step 5: 跑 spike 测试**

Run: `cd /workspace && JAVA_HOME=/root/.local/share/mise/installs/java/17.0.2 ./gradlew :core:dex:jvmTest`
Expected: spike 测试通过。

如果失败,迭代修 `buildMinimalDex` 的 API 调用直到通过。这是本 Task 的核心 —— 验证 dexlib2 能用。

- [ ] **Step 6: 提交 + push**

```bash
git -C /workspace add settings.gradle.kts gradle/libs.versions.toml core/dex/
git -C /workspace commit -m "feat(core:dex): 模块骨架 + dexlib2 API spike"
git -C /workspace push
```

---

## Task 2:DexReader 接口 + 数据类

**Files:**
- Create: `/workspace/core/dex/src/commonMain/kotlin/io/appdex/core/dex/DexReader.kt`

- [ ] **Step 1: 写接口与数据类**

```kotlin
package io.appdex.core.dex

/**
 * DEX 内的类。
 */
data class DexClass(
    /** 类型描述符,如 "Lcom/example/Test;" */
    val type: String,
    /** 简单类名,如 "Test"(从 type 提取) */
    val name: String,
    val accessFlags: Int,
    /** 父类类型描述符,可空(如 java.lang.Object) */
    val superclass: String?,
    /** 实现的接口类型描述符列表 */
    val interfaces: List<String>,
    /** 源文件名,可空 */
    val sourceFile: String?,
    val fields: List<DexField>,
    val methods: List<DexMethod>,
)

data class DexField(
    val name: String,
    val type: String,
    val accessFlags: Int,
)

data class DexMethod(
    val name: String,
    val parameterTypes: List<String>,
    val returnType: String,
    val accessFlags: Int,
)

/**
 * DEX 读取器接口。只读。
 */
interface DexReader {
    /** 列出 DEX 内所有类。 */
    fun listClasses(dexBytes: ByteArray): List<DexClass>

    /** 将指定类反汇编为 smali 文本。 */
    fun toSmali(dexBytes: ByteArray, classType: String): String
}
```

- [ ] **Step 2: 跑 build 确认编译**

Run: `cd /workspace && JAVA_HOME=/root/.local/share/mise/installs/java/17.0.2 ./gradlew :core:dex:build`
Expected: BUILD SUCCESSFUL

- [ ] **Step 3: 提交 + push**

```bash
git -C /workspace add core/dex/
git -C /workspace commit -m "feat(core:dex): DexReader 接口 + 数据类"
git -C /workspace push
```

---

## Task 3:BinaryDexReader 实现(listClasses)

**Files:**
- Create: `/workspace/core/dex/src/jvmMain/kotlin/io/appdex/core/dex/BinaryDexReader.kt`
- Create: `/workspace/core/dex/src/jvmTest/kotlin/io/appdex/core/dex/DexSample.kt`(从 spike 提取)
- Create: `/workspace/core/dex/src/jvmTest/kotlin/io/appdex/core/dex/BinaryDexReaderTest.kt`

- [ ] **Step 1: 把 spike 的 buildMinimalDex 提取到 DexSample.kt**

把 Task 1 spike 测试里验证可用的 `buildMinimalDex` 逻辑提取到独立的 `DexSample.kt`,并扩展支持带字段和方法的类:

```kotlin
package io.appdex.core.dex

// 复用 spike 验证过的 dexlib2 API 构造 DEX 字节
// DexSample 提供:
//   buildMinimalDex()           —— 单空类
//   buildDexWithField()          —— 带一个字段
//   buildDexWithMethod()         —— 带一个方法(空实现)
//   buildDexWithTwoClasses()     —— 两个类
```

具体实现沿用 Task 1 spike 验证过的 API。如果 spike 用了 ImmutableClassDef + DexPool,DexSample 也用同样方式。

- [ ] **Step 2: 写 BinaryDexReader.listClasses**

```kotlin
package io.appdex.core.dex

import org.jf.dexlib2.Opcodes
import org.jf.dexlib2.dexbacked.DexBackedDexFile

class BinaryDexReader : DexReader {

    override fun listClasses(dexBytes: ByteArray): List<DexClass> {
        val dexFile = DexBackedDexFile(Opcodes.getDefault(), dexBytes)
        return dexFile.classes.map { cls ->
            DexClass(
                type = cls.type,
                name = extractSimpleName(cls.type),
                accessFlags = cls.accessFlags,
                superclass = cls.superclass,
                interfaces = cls.interfaces.toList(),
                sourceFile = cls.sourceFile,
                fields = cls.fields.map { f ->
                    DexField(f.name, f.type, f.accessFlags)
                }.toList(),
                methods = cls.methods.map { m ->
                    DexMethod(
                        m.name,
                        m.parameterTypes.toList(),
                        m.returnType,
                        m.accessFlags,
                    )
                }.toList(),
            )
        }.toList()
    }

    override fun toSmali(dexBytes: ByteArray, classType: String): String {
        TODO("Task 4 实现")
    }

    private fun extractSimpleName(typeDescriptor: String): String {
        // "Lcom/example/Test;" -> "Test"
        val inner = typeDescriptor.removePrefix("L").removeSuffix(";")
        return inner.substringAfterLast('/')
    }
}
```

- [ ] **Step 3: 写测试**

```kotlin
package io.appdex.core.dex

import org.junit.jupiter.api.Assertions.*
import org.junit.jupiter.api.Test

class BinaryDexReaderTest {

    @Test
    fun `lists classes of minimal dex`() {
        val bytes = DexSample.buildMinimalDex()
        val reader = BinaryDexReader()
        val classes = reader.listClasses(bytes)
        assertEquals(1, classes.size)
        assertEquals("Lcom/example/Test;", classes[0].type)
        assertEquals("Test", classes[0].name)
        assertEquals("Ljava/lang/Object;", classes[0].superclass)
    }

    @Test
    fun `lists fields`() {
        val bytes = DexSample.buildDexWithField()
        val reader = BinaryDexReader()
        val classes = reader.listClasses(bytes)
        assertEquals(1, classes[0].fields.size)
        // 字段名和类型根据 DexSample 构造的验证
    }

    @Test
    fun `lists methods`() {
        val bytes = DexSample.buildDexWithMethod()
        val reader = BinaryDexReader()
        val classes = reader.listClasses(bytes)
        assertEquals(1, classes[0].methods.size)
    }
}
```

- [ ] **Step 4: 跑测试**

Run: `cd /workspace && JAVA_HOME=/root/.local/share/mise/installs/java/17.0.2 ./gradlew :core:dex:jvmTest`
Expected: 3 个测试通过。

- [ ] **Step 5: 提交 + push**

```bash
git -C /workspace add core/dex/
git -C /workspace commit -m "feat(core:dex): BinaryDexReader listClasses 实现"
git -C /workspace push
```

---

## Task 4:smali 反汇编(toSmali)

**Files:**
- Modify: `/workspace/core/dex/src/jvmMain/kotlin/io/appdex/core/dex/BinaryDexReader.kt`
- Modify: `/workspace/core/dex/src/jvmTest/kotlin/io/appdex/core/dex/BinaryDexReaderTest.kt`

- [ ] **Step 1: 实现 toSmali**

用 dexlib2 的 SmaliWriter 写出 smali 文本:

```kotlin
override fun toSmali(dexBytes: ByteArray, classType: String): String {
    val dexFile = DexBackedDexFile(Opcodes.getDefault(), dexBytes)
    val targetClass = dexFile.classes.first { it.type == classType }
    val sw = StringWriter()
    // smali 输出 API 需要验证,可能是:
    // SmaliWriter(writer).use { it.writeClass(targetClass) }
    // 或 new SmaliWriter(...).writeSmaliClass(targetClass)
    // 参考 dexlib2 源码 org.jf.smali.SmaliWriter
    TODO("用验证过的 SmaliWriter API 写出 smali 文本到 sw")
    return sw.toString()
}
```

API 验证方向:
- `org.jf.smali.SmaliWriter` 构造接受 `Writer`
- `SmaliWriter.writeClass(classDef: ClassDef)` 或 `writeSmaliClass(...)`
- 可能需要 `SmaliOptions` 设置(如 `.registers` vs `.locals`)

如果 SmaliWriter API 难用,备选方案:用 `org.jf.smali.Smali` 的静态方法,或参考 dexlib2 单元测试里的 smali 输出代码。

- [ ] **Step 2: 写测试**

```kotlin
@Test
fun `disassembles class to smali`() {
    val bytes = DexSample.buildMinimalDex()
    val reader = BinaryDexReader()
    val smali = reader.toSmali(bytes, "Lcom/example/Test;")
    assertTrue(smali.contains(".class"))
    assertTrue(smali.contains(".super"))
    assertTrue(smali.contains("Lcom/example/Test;"))
}
```

- [ ] **Step 3: 跑测试**

Run: `cd /workspace && JAVA_HOME=/root/.local/share/mise/installs/java/17.0.2 ./gradlew :core:dex:jvmTest`
Expected: 4 个测试通过。

- [ ] **Step 4: 提交 + push**

```bash
git -C /workspace add core/dex/
git -C /workspace commit -m "feat(core:dex): smali 反汇编(toSmali)"
git -C /workspace push
```

---

## Task 5:detekt + 最终验证

- [ ] **Step 1: 跑全量 build + test + detekt**

Run:
```bash
cd /workspace && JAVA_HOME=/root/.local/share/mise/installs/java/17.0.2 ./gradlew :core:io:build :core:apk:build :core:axml:build :core:arsc:build :core:dex:build detekt
```
Expected: 全部 BUILD SUCCESSFUL

- [ ] **Step 2: detekt 修复(最小)**

- MagicNumber → `@Suppress("MagicNumber")` 类级
- 复杂度 → detekt.yml 调阈值
- 不改业务逻辑

- [ ] **Step 3: 更新 README**

状态部分加:
```markdown
- 子项目 #4: `:core:dex` DEX 只读解析(smali 反汇编) —— ✅ 完成(MVP)
```

- [ ] **Step 4: 提交 + push**

```bash
git -C /workspace add README.md config/detekt.yml core/dex/
git -C /workspace commit -m "quality: Plan 4 全量 detekt + README 更新"
git -C /workspace push
```

---

## 完成标准

- [ ] `:core:dex` 能列出 DEX 类、字段、方法
- [ ] `:core:dex` 能反汇编单类为 smali 文本
- [ ] JVM 单测全过
- [ ] detekt 无 error
- [ ] 所有 task commit + push

## 后续(Plan 1b)

- `:core:ui` + `:feature:files` + `:app` Android UI,需要用户装机验证
